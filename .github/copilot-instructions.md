# Copilot Instructions

Use `AGENTS.md` as the primary stable policy for any task in this repository.

This repository hosts code review agents only.

For stack-specific behavior, follow the selected profile in `.github/agents/`.
